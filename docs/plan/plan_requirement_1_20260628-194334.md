# 项目状态报告：zhangxuefeng-agent

> 生成时间：2026-06-28 19:43:34
> 需求：项目现在怎么样了

## 1. 项目概述

**张雪峰 AI 咨询 Agent** 是一个基于 OpenAI Function Calling + LangChain Agent 构建的智能教育咨询系统，模拟张雪峰的思维方式，为考生和家长提供个性化的升学规划建议。

### 核心功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 高考志愿咨询 | ✅ 正常 | 根据分数、省份、家庭背景给出选校选专业建议 |
| 考研规划 | ✅ 正常 | 择校、择专业、备考策略 |
| 职业规划 | ✅ 正常 | 应届生就业方向、行业选择 |
| 灵魂追问 | ✅ 正常 | 多轮对话自动收集用户画像 |
| AI 驱动 | ✅ 正常 | 基于张雪峰心智模型（SKILL.md）|
| 多步推理 | ✅ 正常 | LangChain Agent 自动串联多个工具调用 |

### 工具系统（7 个工具）

| 工具 | 状态 |
|------|------|
| `search_admission` | ✅ |
| `search_enrollment_plan` | ✅ |
| `search_employment` | ✅ |
| `compare_schools` | ✅ |
| `search_policy` | ✅ |
| `calculate_match` | ✅ |
| `semantic_search` | ✅ |

## 2. 当前状态

### 2.1 Git 状态

```
分支：main
状态：领先 origin/main 6 个提交（待推送）
未提交变更：20 个文件
```

**最近提交（2026-06-28）:**
- `b455793` - chore: record journal
- `4609520` - fix(langchain): resolve streaming duplication, missing args, and system prompt issues
- `a3276bc` - feat: persist app state to localStorage and refactor auto-start message
- `79c0aac` - fix: export ThemeContext and remove stale Vite proxy rewrite

### 2.2 代码统计

| 指标 | 数量 |
|------|------|
| 后端 Python 文件 | 17962 个（包含虚拟环境） |
| 后端业务 Python 文件 | ~50 个 |
| 前端 TypeScript/TSX 文件 | 44 个 |
| 测试文件 | 103+ 个 |
| 文档 | 20+ 篇 |

### 2.3 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | FastAPI + Uvicorn |
| 前端框架 | React 18 + TypeScript + Vite 6 |
| 样式 | Tailwind CSS 3.4（报纸风主题 + 暗色模式） |
| LLM | OpenAI GPT-4o-mini / Anthropic Claude |
| Agent 框架 | LangChain + langgraph |
| 向量搜索 | ChromaDB + bge-small-zh-v1.5 |
| 会话存储 | SQLite（持久化）+ Redis（缓存） |
| 数据库 | SQLite + SQLAlchemy + Alembic |
| 测试 | pytest + Vitest + Playwright |
| 部署 | Docker + GitHub Actions CI/CD |

## 3. 架构概览

```
用户消息
  ↓
POST /chat → 安全校验 → soul_query.py（画像检查）
  ↓
┌─ 传统模式（AgentCore）── OpenAI Function Calling ─┐
└─ LangChain 模式（LangChainAgent）── langgraph ReAct ┘
  ↓
SSE 流式响应 → 会话持久化（SQLite）+ 缓存（Redis）
```

## 4. 待处理工作

### 4.1 待提交的变更（Working Directory）

| 文件 | 变更类型 |
|------|----------|
| `backend/models/admission_score.py` | 模型更新 |
| `backend/schemas/admission_score.py` | Schema 更新 |
| `backend/crud/admission_score.py` | CRUD 更新 |
| `backend/routers/scores.py` | 路由更新 |
| `backend/seeds/import_zhejiang.py` | 新浙江省导入脚本 |
| `frontend/src/components/__tests__/*.tsx` | 测试更新 |
| `tests/test_*.py` | 测试更新 |

### 4.2 未跟踪文件

| 类型 | 数量 | 说明 |
|------|------|------|
| 浙江省数据导入 | 1+ | `backend/seeds/zhejiang_2025/` |
| 真实数据 artifacts | 12+ | `examples/real_data/artifacts/` |
| Pilot 数据快照 | 1+ | `examples/real_data/snapshots/` |
| 文档 | 3+ | `docs/codex-*.md` 等 |

### 4.3 真实数据 MVP 状态

README 披露的当前状态：

```text
source registry -> intake review -> raw snapshot -> parser/rows
  -> quality gate -> dry-run -> loader approval -> artifact manifest
  -> answer source policy -> Agent visibility activation review
```

**默认边界：**
- 不启动爬虫
- 不写真实应用数据库
- 不修改 seed 数据
- 不刷新 RAG/Agent 可见数据

## 5. 快速开始

```bash
# 后端
pip install -e ".[dev]"
uvicorn backend.main:app --reload --port 8000

# 前端
cd frontend && npm install && npm run dev

# Docker
docker compose up -d
```

## 6. 验收命令

| 检查项 | 命令 |
|--------|------|
| 后端健康 | `curl http://localhost:8000/health` |
| 后端测试 | `pytest tests/ -q` |
| 前端构建 | `cd frontend && npm run build` |
| Lint | `ruff check backend/` |
| 格式检查 | `ruff format --check backend/` |

---

**报告生成完毕。**
